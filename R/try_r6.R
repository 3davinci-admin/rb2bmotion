# ToDo: create S3 object ApiKey for store pair b2b_site and b2b_key ----------


ApiKeyCollector <- R6::R6Class("ApiKeyCollector", 
    public = list(
      # add_key methods
    ),
    private = list(
      # TODO: add S3 object ApiKey = list(b2b_site, b2b_key)--------------------
    )
)
    
  
                               
                               